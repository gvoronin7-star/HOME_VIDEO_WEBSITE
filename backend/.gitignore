node_modules/
dist/
.env
.env.*
# The template must be committed: it is the only documentation of which variables
# exist and which ones the server refuses to start without. `.env.*` above would
# otherwise swallow it.
!.env.example
*.sqlite
uploads/
*.log
.DS_Store
